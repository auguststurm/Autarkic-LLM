# Architecture

```text
Host Tavily
  → notes/<slug>/<YYYY-MM-DD>-research-pack.md
  → workflow: Findings (small+medium at once) → Skeptic (small+medium at once) → Report (big)
  → reports/<slug>/<YYYY-MM-DD>-research-report.md
```

`$ROOT` = `$HOME/.pi/agent/skills/search-topic-research-dual-rtx`.

Host owns search. Workflow agents: file tools only.
